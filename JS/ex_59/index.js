let firstNumber = 25;
let secondNumber = 32;
if (firstNumber == secondNumber) {
    console.log('They are the same number');
} else if (firstNumber > secondNumber) {
    console.log('%s is bigger than %s', firstNumber, secondNumber);
} else if (firstNumber < secondNumber) {
    console.log('%s is bigger than %s', secondNumber, firstNumber);
}